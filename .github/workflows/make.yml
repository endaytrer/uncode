name: Make

on:
  push:
    branches: [ "main", "gles" ]
  pull_request:
    branches: [ "main", "gles" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make
