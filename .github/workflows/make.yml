name: Make

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: install 
      run: apt install libadwaita-1-dev libgtk-4-dev libglew-dev
    - name: make
      run: make
